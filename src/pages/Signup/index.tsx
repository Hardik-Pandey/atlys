import AuthPageWrapper from 'common/components/AuthPageWrapper';
import Signup from 'common/components/Signup';

const SignupPage = () => {
  return <AuthPageWrapper Component={Signup} />;
};

export default SignupPage;
