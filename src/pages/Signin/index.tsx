import AuthPageWrapper from 'common/components/AuthPageWrapper';
import Signin from 'common/components/Signin';

const SigninPage = () => {
  return <AuthPageWrapper Component={Signin} />;
};

export default SigninPage;
